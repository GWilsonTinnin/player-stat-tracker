# UVX command to install dependencies and build
# Run with: uv run npm install && uv run npm run build

Install dependencies:
  uv run npm install

Build the plugin:
  uv run npm run build

Watch mode (for development):
  uv run npm run dev

The plugin is an Obsidian plugin that requires Node.js and npm.
uv will manage the Node.js environment for you.
